{
  "scripts": {
    "build": "esbuild bundle_require.js --define:global=globalThis --global-name=__requireCache --bundle --outfile=bundle.js"
  },
  "dependencies": {
    "assert": "^2.1.0",
    "browserify-zlib": "^0.2.0",
    "buffer": "^6.0.3",
    "crypto-browserify": "^3.12.0",
    "events": "^3.3.0",
    "js-yaml": "3.11.0",
    "stream-browserify": "^3.0.0",
    "util": "^0.12.5"
  },
  "devDependencies": {
    "esbuild": "^0.19.5"
  },
  "browser": {
    "stream": "stream-browserify"
  }
}
